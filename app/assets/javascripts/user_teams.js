(function main () {
  "use strict";

  $('#user_team_id').on('chance', function () {
 /*   
    var userTeamId = $(this).val();
    var html =  [
      '<%= form.label :players %><br>',
      '<%= form.collection_select(:id, Team.find_by(id:' + userTeamId +' ).players, :id, :name ) %>' 
      ]
    $('.js-players-container').append(html.join('\n'))


    request.fail(function () {
      var htmlParts = [
        '<div class="alert alert-danger" role="alert">',
        '  There was a problem retrieving the BBQ info. Try again later.',
        '</div>'
      ]
      $bbqContainer.append(htmlParts.join('\n'))
    })

    request.done(function (players) {
      var htmlParts = "<dl class='dl-horizontal'>"
      items.forEach(function (item) {
        htmlParts = htmlParts + 
          '<dt>' + item.name + ': </dt><dd>' + item.quantity + '</dd>'
        })
        htmlParts = htmlParts + '</dl>' 
      
      $bbqContainer.append(htmlParts)
    });
*/

  }


})();